package com.solace.ui.theme

import androidx.compose.ui.graphics.Color

// Brand
val SolaceTeal = Color(0xFF4ECDC4)
val SolaceTealDark = Color(0xFF38B2AA)

// Backgrounds
val BackgroundDark = Color(0xFF120B3C)   // splash / dark screens
val BackgroundLight = Color(0xFFFFFFFF)
val SurfaceLight = Color(0xFFF8F8F8)

// Text
val TextPrimary = Color(0xFF0A0A0A)
val TextSecondary = Color(0xFF6B6B6B)
val TextOnDark = Color(0xFFFFFFFF)
val TextOnPrimary = Color(0xFFFFFFFF)

// Chips / selection
val ChipSelectedBg = SolaceTeal
val ChipUnselectedBg = Color(0xFFF0F0F0)
val ChipSelectedText = Color(0xFFFFFFFF)
val ChipUnselectedText = TextPrimary

// Dividers / borders
val Divider = Color(0xFFE0E0E0)
val InputBorder = Color(0xFFD0D0D0)

// Nav
val NavActive = SolaceTeal
val NavInactive = Color(0xFF888888)

// Status / error
val Error = Color(0xFFFF4444)

// Milestone screen backgrounds
val MilestoneCoral = Color(0xFFE8705A)
val MilestoneTeal = Color(0xFF5BC8B5)
val MilestoneNavy = BackgroundDark

// Mood palette (maps to MoodColor enum in UiConfigModels)
val MoodCalmRelaxed = SolaceTeal
val MoodEnergeticPassionate = Color(0xFFFF6B35)
val MoodHappyOptimistic = Color(0xFFFFD93D)
val MoodNorthReflective = Color(0xFF4A5568)
val MoodPeacefulGrounded = Color(0xFF6BCB77)
val MoodConfusedOverwhelmed = Color(0xFF9B72CF)
val MoodNeutralNumb = Color(0xFF9E9E9E)
val MoodWarmHopeful = Color(0xFFFF8FAB)
